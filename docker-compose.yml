version: "3"
services:

    database:
        container_name: database
        image: mysql:5.6
        ports:
            - "3306:3306"
        volumes:
            - ~/.data/mysql:/var/lib/mysql
        environment:
            - MYSQL_ROOT_PASSWORD=password
